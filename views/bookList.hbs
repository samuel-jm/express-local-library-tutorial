<h1>{{title}}</h1>
{{#if book_list.length}}
  <ul>
    {{#each book_list}}
      <li>
        <a href={{this.url}}>
          {{this.title}}
        </a>
        ({{this.author.name}})
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>There are no books.</p>
{{/if}}