<h1>{{title}}</h1>
{{#if genre_list.length}}
  <ul>
    {{#each genre_list}}
      <li>
        <a href={{this.url}}>
          {{this.name}}
        </a>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>There are no genres.</p>
{{/if}}