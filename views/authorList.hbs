<h1>{{title}}</h1>
{{#if author_list.length}}
  <ul>
    {{#each author_list}}
      <li>
        <a href={{this.url}}>{{this.name}}</a>
        ({{this.lifespan}})
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>There are no authors.</p>
{{/if}}